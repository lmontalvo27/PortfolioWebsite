function toggleMenu() {
    const menu = document.quetSelector(".menu-links");
    const icon = document.quetSelector(".hamburger-icon");
    menu.classList.toggle("open");
    icon.classList.toggle("open");
}